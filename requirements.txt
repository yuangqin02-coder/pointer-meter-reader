# Core dependencies for pointer meter reading
# Tested on Python 3.10+ with opencv-python 4.x and ultralytics 8.x

numpy>=1.24.0
opencv-python>=4.8.0
ultralytics>=8.0.0

# Optional but recommended
matplotlib>=3.7.0
pandas>=2.0.0
